# 米诺 V3 · 上线送审清单（M6）

> 状态：送审前逐项核对清单（可勾选）。
> 范围：微信小程序类目/资质、生成式 AI 合规、隐私与协议、域名与备案、订阅消息、真机回归、FastGPT 生产部署。
> 说明：`[ ]` 未完成 / `[x]` 已完成。带「阻断」标记的项未过则无法提审或无法上线。

---

## 1. 微信小程序类目与资质（AI 对话类目）

- [ ] **阻断** 小程序主体已完成企业/个体认证（个人主体通常无法开通 AI 对话相关类目，需企业资质）。
- [ ] **阻断** 服务类目已选到「AI 对话」相关目类（如「效率工具 / 商业服务」下与「人工智能」「文字对话」匹配的目类；以微信最新《非个人主体小程序开放的服务类目》为准）。
- [ ] 已按类目要求提交**行业资质证明**（AI 对话/生成式内容服务往往要求相应经营许可或承诺函）。
- [ ] 小程序名称、简介、Logo 不含误导性「算命/占卜/命理」表述（本产品定位战略参谋，不做命理排盘，避免踩「封建迷信」红线）。
- [ ] 客服渠道已配置（微信后台「客服」或小程序内联系方式），满足审核对用户求助入口的要求。

## 2. 生成式 AI 备案 / 申报

- [ ] **阻断** 已完成**生成式人工智能服务**合规路径评估：面向公众提供服务需按《生成式人工智能服务管理暂行办法》履行**算法备案**（互联网信息服务算法备案系统）与（视情况）**大模型上线备案/安全评估**。
- [ ] 底层模型来源合规：所接入模型（产品方自行接入的 API/密钥）具备合法资质或已在境内完成相应备案；留存模型供应商合规证明。
- [ ] 已准备《算法备案》材料：算法基本情况、安全自评估报告、拟公示的算法信息。
- [ ] 语料与训练数据合规说明（若涉及自建知识库回喂：FastGPT 每用户知识库仅存该用户自述业务信息，不含第三方个人信息）。

## 3. 深度合成 / 生成内容标识义务

- [ ] **阻断** 生成内容**显著标识**：米诺回复与报告页在显著位置标注「本内容由 AI 生成」（对话页气泡区/报告详情页页脚）。
- [ ] 生成内容**隐式标识**：导出分享图（决策 5，端上 canvas 绘制）在图上保留「AI 生成」字样与品牌署名 `米诺战略参谋部`，满足《互联网信息服务深度合成管理规定》对传播态内容的标识要求。
- [ ] 不得用于生成违法信息：已接入内容安全双向审核（见 §7）。
- [ ] 保留生成日志（对话/报告落库）以备溯源，留存周期符合监管要求。

## 4. 隐私政策与用户协议

- [ ] **阻断** 登录页/入局页提供《用户协议》《隐私政策》勾选（未勾选不可登录；方案 R7）。
- [ ] 隐私政策**收集项清单**逐项列明并说明用途：
  - [ ] `openid`（微信登录标识，用于账号识别）
  - [ ] `nickname` 昵称（展示与个性化称呼）
  - [ ] `industry` 行业（战略建议个性化）
  - [ ] `bizNote` 生意背景（战略建议个性化）
  - [ ] 对话内容 `messages`（提供对话服务、生成报告、构建每用户知识库）
- [ ] 说明**数据处理者与委托处理**：对话内容会传输至自托管 FastGPT 及其所接入的模型服务商用于生成回复。
- [ ] **删除权**：明示用户可注销账号并删除全部数据，入口对应后端 `DELETE /me`（级联删除 conversations/messages/reports/tasks + 删除 FastGPT 知识库；已实测幂等清空）。
- [ ] 已按《移动互联网应用程序个人信息保护管理规定》完成小程序侧隐私接口权限声明（`wx.getUserProfile` 等，如使用）。
- [ ] 未成年人保护声明（本产品面向经营者，声明不面向未成年人）。

## 5. 域名、备案与合法域名（request）

- [ ] **阻断** 后端 API 域名已完成 **ICP 备案**，HTTPS 证书有效。
- [ ] **阻断** 微信小程序后台「开发管理 → 服务器域名」已配置：
  - [ ] `request` 合法域名（后端 API 域名）
  - [ ] SSE 走 `wx.request` + `enableChunked`（同属 request 域名，方案 R1；无需 socket 域名）
  - [ ] 如加载子集化 Noto Serif SC 字体：`downloadFile` 合法域名 / 字体 CDN 域名（方案 R3）
- [ ] 生产环境 `.env` 已用真实 `WX_APPID/WX_SECRET`，`WX_MOCK=false`、`FASTGPT_MOCK=false`。
- [ ] 内容安全 msgSecCheck 复用上述 access_token（同 appid/secret），无需额外域名。

## 6. 订阅消息模板申请（两模板）

- [ ] **阻断** 已在微信后台申请两个一次性订阅消息模板，并把模板 id 填入 `WX_TMPL_DAILY_Q` / `WX_TMPL_REPORT_READY`：
  - [ ] **今日一问**模板：建议字段 `thing1`（问题摘要，≤20 字）、`time2`（推送时间）。
  - [ ] **报告完成**模板：建议字段 `thing1`（报告标题，≤20 字）、`time2`（完成时间）。
- [ ] 端上在「开始聊」「生成报告」两个高意愿时机请求订阅授权（一次可勾选多模板，方案 R4）。
- [ ] 无授权时报告完成仅站内未读态兜底（后端推送失败绝不抛出，已容错）。
- [ ] `WX_PUSH_PAGE_CHAT` / `WX_PUSH_PAGE_REPORTS` 跳转页与实际路由一致；生产 `miniprogram_state=formal`。

## 7. 内容安全接入（R7，本期已实装）

- [ ] **阻断** 用户输入审核：发消息入口 `msgSecCheck` 命中 → 400「这段话我不能收，换个说法」，不落库、不调 LLM（已实测）。
- [ ] **阻断** LLM 输出审核：流结束后审 assistant 全文，命中 → 落库替换为「（这段话米诺收回了）」+ 下发 `event: retract`（端上把该气泡整条替换；契约见 ChatService.SseEvent 注释）。
- [ ] 报告正文审核：Worker 在 ready 前审 `bodyMd`，命中置 `failed`（已单测覆盖）。
- [ ] **阻断（生产建议）** 审核失败策略：生产环境按合规要求评估是否将 `SAFETY_FAIL_CLOSED=true`（严格模式：审核接口不可用时拦截）；默认 fail-open 仅适合灰度/联调。
- [ ] msgSecCheck v2 要求 openid 近 2 小时内使用过小程序，已用当前登录用户 openid 传入。

## 8. 真机回归清单

- [ ] **阻断** R1 SSE 流式：iOS 与 Android 真机各验证一次「发消息→逐字流出→suggestions/reportOffer→done」，无分包边界乱码（UTF-8 拼接、`\n\n` 切帧）。
- [ ] retract 事件真机验证：构造命中审核的输出（或联调触发），确认气泡替换为撤回文案。
- [ ] R3 衬线字体：`wx.loadFontFace` 加载成功与失败降级两条路径，版式不崩。
- [ ] canvas 导出分享图：`GET /reports/:id/export` 结构 → 端上绘制宣纸风格长图 → 保存相册 / 转发微信，含「AI 生成」标识与品牌署名。
- [ ] 订阅消息弹窗：两时机请求授权，勾选后次日/报告完成能收到推送并点回小程序。
- [ ] 异常态/空态：空对话、空报告库、报告 generating/failed、网络断流「米诺正在闭关」友好态。
- [ ] 账号删除：设置页触发 `DELETE /me` → 二次确认 → 退出登录，数据不可再访问（旧 token 401）。

## 9. FastGPT 生产部署核对

- [ ] **阻断** 模型 API key 已在 FastGPT 配置并可用（对话模型、向量模型 `FASTGPT_KB_VECTOR_MODEL`、agent 模型 `FASTGPT_KB_AGENT_MODEL`）。
- [ ] **阻断** `config.json` 中登记的模型名与后端 `.env` 的 `FASTGPT_KB_*` 一致（否则建库失败）。
- [ ] **阻断** Mongo 以**副本集**方式部署（FastGPT 事务依赖副本集，单实例会报错）。
- [ ] 米诺应用（V6.0 浓缩提示词）与报告工作流已在 FastGPT 配置并生成 `FASTGPT_APP_KEY`；`FASTGPT_OPENAPI_KEY` 具备知识库建/删权限。
- [ ] 知识库删除端点连通性验证：`DELETE /api/core/dataset/delete?id=` 生产可达（合规删除依赖）。
- [ ] 后端对 FastGPT 调用的超时/重试/熔断按 R9 生效；报告 job 失败可重试。
- [ ] Redis 生产可用；不可用时 report-generate 走同步降级、kb.ingest 空操作（已容错，但生产应保证 Redis 在线）。

## 10. 提审前最终核对

- [ ] `lint` / `build` / `test` 全绿。
- [ ] 生产 `.env` 无 mock、无占位；密钥不入库、不进日志。
- [ ] 体验版走查一遍全链路：登录→入局→对话→报告→导出→推送→删除。
- [ ] 提交审核备注中说明：AI 生成内容已标识、已接入内容安全审核、提供数据删除入口。
